import styled from 'styled-components';
import { Form } from "antd";

const ButtonWrapper = styled.div`
  margin-top: 10px;
`

const FormWrapper = styled(Form)`
  padding: 10px;
`

export { ButtonWrapper, FormWrapper }